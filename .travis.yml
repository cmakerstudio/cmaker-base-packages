---
language: python
dist: xenial

before_install:
  # Make sure everything's up to date.
  - sudo apt-get update -qq
  - sudo apt-get install python3-pip libssl-dev python3-setuptools
  - sudo pip3 install molecule

install:
  # Install Ansible.
  - pip3 install ansible

script:
  - sudo molecule test